# From: https://stackoverflow.com/a/52295765

# Ignore everything (and all subdirectories recursively) in the home directory
/*
*/*

# Track only these files in home directory
!.bash_aliases
!.bash_fancyprompt
!.tmux.conf
!.gitignore
